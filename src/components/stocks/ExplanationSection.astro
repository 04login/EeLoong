---
// Re-export ExplanationModal for compatibility
import ExplanationModal from "./ExplanationModal.astro";

interface Props {
  sections?: ("valuation" | "segments" | "audit" | "peers" | "fairvalue")[];
  showGuideBar?: boolean;
}

const { showGuideBar = true } = Astro.props;
---

<ExplanationModal showGuideBar={showGuideBar} />